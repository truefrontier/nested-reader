# Goal

Ship Nested Reader Astra so Mac downloads from Downloads work (no App Translocation crash), public nestedreader.app/app is install guidance (not a broken web reader), Gatekeeper install steps with a screenshot sit next to Download for Mac, and auth supports paste-API-key or local Codex API-key import (no faked ChatGPT-subscription API access, no committed sk- secrets).
